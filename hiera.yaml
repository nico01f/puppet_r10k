---
:hierarchy:
  - "nodes/%{::fqdn}"
  - "datacenters/%{::dc}"
  - "applications/%{::hostname}"
  - "applications/%{domain}"
  - "roles/%{::role}"
  - users
  - common
:backends:
  - yaml
:yaml:
  :datadir: /etc/puppet/hieradata
